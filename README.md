Q.1. Whats is the difference between let, var and const?
Answar: the main difference between le, var and const are ;
  var:function scoped, can be redeclared, can be reassigned
  let:block scoped, can not be redeclared, can be reassigned
  const:block scoped, cant not be redeclared, can nor be reassigned

Q.2. What is the spread operator?
answar: spread operator is often used to copy new array. (...) by using this a new array can be create and it dosent change the original array.

Q.3. What is the difference between map(), filter(), and forEach()?
Answar: the difference are;
   forEach(): forEach is used to loop through an array and do something, but it does not return a new array.
   map(): map is used to transform each element of an array and returns a new array.
   filter(): map is used to select elements based on a condition and returns a new array.


Q.4. Whats is an array function?
Answar: An Arrow Function is a shorter and modern way to write functions in JavaScript.
It was introduced in ES6 (2015) and uses the => symbol.


Q.5.  What are template literals?
Answar: Template Literals are a modern way to write strings in JavaScript.backticks () instead of quotes.
They make it easier to insert variables and create multi-line strings.
